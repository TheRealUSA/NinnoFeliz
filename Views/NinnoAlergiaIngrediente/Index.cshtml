@model IEnumerable<NinnoFeliz.Models.NinnoAlergiaIngrediente>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdAlergiaNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdIngredienteNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdNinnoNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdAlergiaNavigation.NombreAlergia)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdIngredienteNavigation.NombreIngrediente)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdNinnoNavigation.NombreNinno)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdNinnoAlergiaIngrediente">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdNinnoAlergiaIngrediente">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdNinnoAlergiaIngrediente">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
