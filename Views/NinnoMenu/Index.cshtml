@model IEnumerable<NinnoFeliz.Models.NinnoMenu>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FechaConsumido)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdNinnoNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdNumeroMenuNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FechaConsumido)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdNinnoNavigation.NombreNinno)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdNumeroMenuNavigation.NombreMenu)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdNinnoMenu">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdNinnoMenu">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdNinnoMenu">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
