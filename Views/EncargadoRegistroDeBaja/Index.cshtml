@model IEnumerable<NinnoFeliz.Models.EncargadoRegistroDeBaja>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdEncargadoNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdRegistroBajaNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdEncargadoNavigation.Apell1Encargado)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdRegistroBajaNavigation.IdRegistroBaja)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdEncargadoRegistroBaja">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdEncargadoRegistroBaja">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdEncargadoRegistroBaja">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
